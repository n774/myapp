<h1>グループ管理画面</h1>

<table>
  <tr>
    <th>G</th>
    <th>G name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @groups.each do |group| %>
  <tr>
    <td><%= group.g_id %></td>
    <td><%= group.g_name %></td>
    <td><%= link_to '詳細表示', group %></td>
    <td><%= link_to '編集', edit_group_path(group) %></td>
    <td><%= link_to '削除', group, method: :delete, data: { confirm: '本当に削除してもよろしいですか?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to '新規作成', new_group_path %>
